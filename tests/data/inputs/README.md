# inputs

- Content: stable test inputs
- Type: input assets
- Purpose: preserve reusable configs and maps outside time-stamped result archives

## Notes

- Stable `RackeTree` / `DSL` base configs live under `configs/`.
- The project-level reproduction guide is `doc/methodology_racke_dsl_repro.md`.
